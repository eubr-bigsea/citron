{{#each tasks as |task|}}
  {{workflows/workflow-task
  readOnly=readOnly
  operations=operations
  elementId=task.id
  jsplumb=jsplumb
  workflow=workflow
  task=task
  clickTask=(action "clickTask")
  addFlow=(action "addFlow")
  removeFlow=(action "removeFlow")
  removeTask=(action "removeTask" task)
  }}
{{/each}}
