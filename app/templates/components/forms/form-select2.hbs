<select onchange={{action 'valueChanged'}} class="form-control" style="width: 100%">
  <option></option>
  {{#each parsedValues as |value|}}
    <option selected={{value.selected}} value={{value.value}}>{{value.key}}</option>
  {{/each}}
</select>
