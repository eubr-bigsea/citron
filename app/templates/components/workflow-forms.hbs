{{#each forms as |form|}}
  <fieldset>
    <legend>{{form.name}}</legend>
    {{#each form.fields as |field|}}
      {{form-label field=field}}
      {{component (concat 'form-' field.suggested_widget) field=field onChange=(action 'formChanged')}}
    {{/each}}
  </fieldset>
{{/each}}
