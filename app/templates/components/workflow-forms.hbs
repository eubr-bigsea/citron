{{#each forms as |form|}}
  <fieldset>
    <legend>{{form.name}}</legend>
    {{#each form.fields as |field|}}
      {{log field.suggested_widget}}
      {{component (concat 'form-' field.suggested_widget) field=field}}
    {{/each}}
  </fieldset>
{{/each}}
